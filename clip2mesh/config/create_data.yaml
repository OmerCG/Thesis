defaults:
  - renderer_kwargs: smplx

output_path: /home/nadav2/dev/data/CLIP2Shape/images/tall_data # Path to save the images
gender: male
img_tag:
model_type: smplx
with_face: True
num_of_imgs: 1000
num_coeffs: 10
sides: True
# texture: /home/nadav2/dev/repos/CLIP2Shape/Flame/flame2020/mesh.png  # FLAME texture
texture: /home/nadav2/dev/repos/CLIP2Shape/SMPLX/textures/smplx_texture_m_alb.png  # SMPLX texture
# texture:  # SMAL texture (keep empty for random texture)

renderer:
  name: pytorch3d

renderer_kwargs:
  img_size: [1024, 1024]
  tex_path: ${texture}
  background_color: [0.0, 0.0, 0.0]