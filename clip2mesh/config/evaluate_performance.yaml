defaults:
    - renderer_kwargs: smplx

out_path: /home/nadav2/dev/data/CLIP2Shape/outs/descriptors_ablations/smplx_male
model_path: /home/nadav2/dev/repos/Thesis/end2end_pipeline/smplx/smplx_male_e2e.ckpt
min_value: 10.0
max_value: 40.0
effect_threshold: 0.3
color_map: YlOrRd
gender: male
method: L2

optimize_feature: betas
model_type: smplx

renderer_kwargs:
    img_size: [1024, 1024]
    texture_optimization: True
    model_type: ${model_type}